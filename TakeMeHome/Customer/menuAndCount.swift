class menuAndCount {
    var name : String?
    var count : Int?
    var menuId : Int?

    init(count: Int, name: String, menuId: Int) {
        self.name = name
        self.count = count
        self.menuId = menuId
    }
}
